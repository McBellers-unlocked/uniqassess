# AWS practical lab: pilot and follow-on plan

Status: the dedicated AWS account, private control runtime and candidate exercise are deployed. The live synthetic AWS operator run passed **24 checks** on 23 September 2026, including release, rollback, real alarm transitions and independently verified cleanup/expiry. Application deployment **Amplify build 77**, revision `782f825`, succeeded. Alpha's candidate/assessor browser check and Beta's closed-browser lab expiry, reopening and assessor evidence passed, with independent resource absence and the lifecycle limits below. Administrative candidate finalization remains request-driven, separately from scheduled lab expiry. The activated assessment is restricted to its named fictional pilot identities. Human review, engineer calibration, load testing and cost measurement remain pending. See the [browser pilot evidence](DEVOPS_BROWSER_PILOT.md), [operator evidence](AWS_LAB_OPERATOR_ACCEPTANCE.md) and [deployment report](AWS_LAB_DEPLOYMENT.md).

The first AWS exercise now uses a small Lambda/S3/CloudWatch service, prepared Terraform and a recorded release pipeline running in AWS CodeBuild. A dedicated account is reserved exclusively for one candidate lease. This follows the Kubernetes technical pilot and the user's instruction to set up both main assessment tasks; it does not substitute for the subsequent engineer study. EKS, unrestricted cloud administration and production integrations remain outside the first release.

## Implemented pilot

AWS Organizations member account `689324611808` is separate from the existing hosting/billing account `891612540396` and has one exclusive lease at a time. A private management Lambda owns DynamoDB session/job state and assumes a scoped role in the child account. Each candidate receives a fixed service, application role, CodeBuild role/project, private S3 workspace and log groups. Candidate work runs asynchronously in managed CodeBuild containers, with a five-minute build limit and one active job. The protected permissions boundary binds access to exact lease resources, principal roles and deadline; completed or uncertain cleanup determines whether the account can be reused. [Provisioning and boundary evidence](AWS_LAB_PREFLIGHT.md)

Task 1 is Kubernetes recovery worth 40 marks; Task 2 is AWS release worth 60. The assessment has one 100-minute timer with suggested 40/60-minute allocation. Terraform repair, pipeline execution, rollback and request-level performance/alert evidence provide distinct scored evidence. Azure is not directly assessed. [Assessment content and rubric](DEVOPS_ASSESSMENT.md)

The initial runtime is internet-connected and has no AWS-endpoint-only outbound allowlist. It limits resources, concurrency and time but provides no exact currency spending cap. Keep these constraints explicit during the controlled pilot. [Deployment limits and browser acceptance status](AWS_LAB_DEPLOYMENT.md)

Candidate fixture versioning is also a pilot limitation: its S3 bucket retains versions, but the current source key is mutable and the runtime requests the latest object. Record the checksum and S3 version used for each technical/browser test. Before hiring cohorts, bind each assessment template to an immutable fixture key or explicit object version, so frozen assessment content includes the executable exercise as well as the written brief.

## Current AWS acceptance and remaining gates

The completed operator run used only synthetic resources and retained **15 successful command jobs**. It verified the initial service fault, persistent files, identical-command reuse, conflicting-command rejection, selected forbidden AWS actions, a failed-test deployment gate, Terraform/release operation, valid and missing-order responses, measured dependency reads, metric-driven alarm breach/recovery, known-good rollback, explicit stop and independent deadline cleanup. Both lease inventories were empty at the independent observation and no cleanup failed. This is the bounded matrix actually exercised, not an exhaustive security audit. [Full sanitized observations and report identity](AWS_LAB_OPERATOR_ACCEPTANCE.md)

The two Ready observations were **88.158 and 88.785 seconds**. Command jobs took **37.653–122.195 seconds**, including container/tool startup and polling; the release pipeline took **111.486 seconds** and rollback **75.803 seconds**. These are single-run observations, not p95 values or a candidate completion-time study. Five repaired requests each used one dependency read instead of three, but the first repaired request was slower than all baseline samples; the retained data does not establish a guaranteed speedup.

The combined synthetic Alpha browser check submitted at **13:31:47.900 UTC** on 23 September 2026. Three successful Kubernetes commands recovered **2/2 Ready replicas** and verified **HTTP 200**. Two successful AWS jobs ran seven unit tests, observed the expected seeded service fault, retained a workspace marker and initialized/validated Terraform. Reload retained the written response and AWS job history; the assessor view displayed both submitted notes and all five command/job records without changing scores. This AWS browser check covers integration and persistence; the full release/rollback work was verified in the operator run.

The AWS controller recorded `cleanedAt` **13:31:53.401 UTC**. Both application cleanup receipts were subsequently saved about **40.9 seconds after the submission lock**; that later synchronization interval is not measured resource-deletion duration. The assessor view honestly marked the Kubernetes snapshot truncated at 32,768 characters; the 667-character AWS snapshot was untruncated. Independent inspection found all eight exact AWS resources absent at **13:37:32.650 UTC** and the exact Kubernetes namespace absent at **13:37:38.831 UTC**, with the target cluster and broker verified. These are observation times, not measured resource-deletion instants.

The six-minute Beta AWS browser was closed at **13:36:30.837 UTC** while a job was running, before its **13:39:47.540 UTC** deadline. The job completed successfully at **13:37:44.740 UTC**. With the browser still closed, scheduled lab expiry retained a snapshot at **13:39:51.415 UTC** and the controller recorded `cleanedAt` **13:39:53.550 UTC**. The application cleanup receipt was subsequently saved at **13:40:44.106 UTC**, **56.566 seconds after the deadline**; this measures application synchronization, not actual deletion duration. Read-only database inspection observed expired lab state and retained evidence at **13:40:53.733 UTC** before any page/controller read. The independent helper found all eight exact resources absent at **13:41:24.109 UTC**, without invoking cleanup. Reopening the page after that proof, at **13:42:03 UTC**, showed completion. The assessor verified the autosaved 44-word note, expired lab, one successful job and retained snapshot/cleanup evidence. [Detailed browser observations](DEVOPS_BROWSER_PILOT.md)

The candidate row remained `started` while the browser was closed. Existing candidate loading finalized administrative `submittedAt` and `workLockedAt` at **13:42:03.546 UTC** on return; the original cleanup receipt remained unchanged. The assessor displayed **8 minutes for the 6-minute attempt** because elapsed time uses that later administrative timestamp. Deadline gates and scheduled lab cleanup operated independently: the work deadline was not extended. This is a pass for browser-independent lab expiry, not evidence of scheduled candidate submission. Resolve or explicitly accommodate stale dashboard state and administrative elapsed-time reporting before wider hiring use, keeping effective deadline, evidence retention, resource cleanup and administrative submission timestamps distinct.

The temporary operator verification helper was removed after the checks at **13:44:21.187 UTC**. The deployed runtime remained healthy and the account pool had no active lease. The helper-removal receipt is retained in [bootstrap cleanup evidence](../infra/aws-labs/bootstrap-cleanup-evidence.json); the independently scheduled product cleanup remains installed.

The frozen assessment's `controlledPilot.syntheticOnly` marker restricts it to the two predefined fictional technical checks and blocks ordinary cohort creation, psychometric programmes and candidate imports. The recorded publication override is an operator-test authorization, not human assessment approval. A supervised engineer study requires a separately reviewed version, immutable executable fixture binding and genuine review records. Keep the provisional 100-minute assessment time until engineer timing and marking have been calibrated.

## What the Kubernetes pilot has established so far

The Kubernetes infrastructure has now been provisioned and tested: a two-node, single-control-node pilot in a dedicated VPC within the existing AWS account, limited to two concurrent labs. Actual gVisor `runsc` sandbox/gofer processes were observed using `systrap`; the worker has no IAM instance profile and its metadata endpoint is disabled. The final image/configuration passed **43 of 43 live isolation, workload-repair and cleanup checks** across two disposable environments. Control and worker service restarts preserved the checked runtime, placement, DNS and host-guard configuration. [Recorded infrastructure evidence](../infra/kubernetes-labs/PILOT_VERIFICATION.md)

Both final image scans completed with **0 critical, 2 high, 2 medium and 1 low findings per image**. Component review found the formatter implicated by one high finding absent; the zlib high finding remains recorded. These results describe the scanned digests and tested controls, not an absence of vulnerabilities or a general security certification. Image changes need new scans and relevant live checks. [Final image findings and limitations](../infra/kubernetes-labs/PILOT_VERIFICATION.md#final-image-validation)

The broker lifecycle/recovery suites, synthetic Bravo application API checks, Alpha's browser repair/submission and Alpha's assessor evidence view have also passed. Bravo reached Ready in **6.48 seconds with the cluster already running and images preloaded**, retained four command records after submission, and received an application cleanup receipt 62.756 seconds after its work lock. A five-minute lab expired with its browser closed; read-only inspection found its command/snapshot retained and cleanup receipt saved **26.585 seconds after the deadline**, without candidate or assessor access triggering cleanup. Independent cluster inspection subsequently confirmed all three candidate namespaces absent and scheduled reconciliation reported zero failures. These are single operator observations, not a supervised human engineer pilot or latency percentiles: candidate usability, cold-start/p95 performance, reliable cohort capacity, total billed cost per completed assessment, marking agreement and pass thresholds remain unknown. See the [deployment report](KUBERNETES_PILOT_DEPLOYMENT.md) for the measured observations and operating constraints.

| Observation or implementation finding | Consequence for the AWS design | Evidence still needed |
| --- | --- | --- |
| The two-node Kubernetes pilot passed 43 live checks with real gVisor, admission controls, network restrictions, workload repair and namespace cleanup. | Require observed enforcement and exercise repair in the actual AWS sandbox design; configuration files alone are insufficient evidence. | Selected AWS IAM/API denials and independent account-resource cleanup now pass. Copied-credential enforcement and the broader abuse matrix remain distinct checks; Kubernetes evidence does not validate AWS identities. |
| The Kubernetes pilot uses a separate VPC in the hosting account. The follow-on AWS implementation now has one dedicated Organizations member account. | Keep AWS candidate credentials and workloads inside the exclusively leased child account; orchestration remains outside it. | Live account reset/reuse and copied-credential enforcement, followed by capacity/cost evidence before growing the account pool. |
| Image remediation reduced critical scan findings to zero on the final digests, while reviewed high findings remain. | Pin artifacts, retain component-level findings and an update process, and repeat affected isolation checks after rebuilds. | Ongoing vendor fixes, rebuilt-image verification and an explicit risk decision before broader candidate use. |
| A lab can be attached to an existing written task using a versioned preset. The candidate can switch between the console and their deliverable. | Reuse that task model and keep the written explanation alongside practical evidence. Freeze the AWS template version for each assessment. | Whether candidates can find the lab, understand instructions and move between evidence and writing without losing time. |
| The app sends authenticated requests to a separate runner; candidate-cookie isolation passed live API tests. Tested Kubernetes namespaces denied cross-namespace network/API access and tested host/metadata/internet paths. | Keep orchestration credentials outside the candidate environment and expose only a session-owned lab/job API. | Equivalent AWS account/session tests, runner compromise tests and actual AWS network/IAM boundary tests. |
| Live broker/application checks retained one execution for a repeated request ID, rejected conflicting text and concurrent commands, preserved an idle lab across restart, and failed an interrupted command without replay. | Reuse durable session/job identities and reconciliation. A disconnected browser must not duplicate a Terraform apply or deployment. | Equivalent lost-response, cancellation/restart and partial-apply cases for real AWS provisioning jobs; short Kubernetes batches do not prove asynchronous cloud-job behaviour. |
| Kubernetes commands use fresh shells, persistent workspace files, a 20-second limit and bounded output. The runner closes the lab after a command times out. | The AWS implementation uses separate durable asynchronous jobs with bounded duration and recovery. Its observed release job took 111.486 seconds including startup and polling, so the Kubernetes limit would not fit this flow. | Larger timing samples, failure/recovery cases and human evidence that the batch console supports the task without disproportionate lost time. |
| The assessor UI displayed retained commands, outputs, the written response, final snapshot and cleanup receipt. Both observed final snapshots reached the retention cap with truncation visible. Candidate-controlled output is not a trusted score. | Capture concise, exercise-specific independent cloud state and release checks alongside transcripts; avoid a verbose inventory crowding important evidence out of retention limits. Score diagnosis, decisions and verification. | Blind marking agreement, useful snapshot fields, justified alternative repairs, and ways candidates can satisfy superficial checks without fixing the problem. |
| Live submission locked new commands; the closed-browser lab expired and retained evidence without candidate access; an independent janitor removed an expired namespace while the broker was offline. | Submission must stop new work, revoke cloud access, preserve evidence and schedule independently retried cleanup. Browser activity cannot be the cleanup clock. | AWS-specific submission/expiry races, unreachable services, partially created resources, copied-credential revocation delay and complete account reset. |

Implementation references: [lab configuration](../src/lib/recruit/kubernetes-lab-config.ts), [application lifecycle](../src/lib/recruit/kubernetes-lab-service.ts), [runner](../lab-runner/runner.py), and [Kubernetes exercise](../lab-runner/exercises/kubernetes-troubleshooting-v1/README.md). The completed technical acceptance is recorded in [sanitized evidence](../infra/kubernetes-labs/acceptance-evidence.json); the subsequent human-pilot gates remain distinct.

## First AWS exercise

Implemented template: `aws-service-release-v1`, with a suggested 60 minutes within the shared 100-minute two-task assessment. Duration and pass marks must be calibrated with practising engineers.

The candidate inherits a small order-status service. A recent release fails its acceptance checks. They receive a working directory containing pinned Terraform, application code, tests, a pipeline definition, an architecture diagram and a short incident brief. The sandbox is already provisioned with account guardrails, a deployment role, an application role, a private S3 bucket and a baseline Lambda service. It contains fictional data only.

The candidate must:

1. Inspect the existing service, logs, configuration and proposed Terraform changes; identify the likely cause with evidence.
2. Repair the bounded infrastructure/application configuration and supply a safe Terraform plan. The exercise should include an incorrect S3 object prefix or IAM resource scope and a release configuration fault; publish exact seeded defects only in assessor material.
3. Run a pipeline that tests, plans, deploys a version, performs a service-level smoke test and preserves a usable rollback. Its execution must be real and recorded. A YAML review alone is recorded as a review, not proof of successful pipeline operation.
4. Demonstrate the repaired service, an appropriate failure signal in CloudWatch and a verified rollback to the supplied known-good version.
5. Explain the changes, least-privilege decision, deployment order, remaining risk and how the architecture would change under higher load or a second dependent service.

Use Lambda, S3 and CloudWatch for the initial bounded environment. Allow edits to a specific application role policy within an immutable permissions boundary; do not allow candidates to change their own deployment identity or trust relationships. Keep the public interface optional: direct authenticated invocation is sufficient initially. If an HTTP endpoint is part of the task, require `AWS_IAM` authentication and signed test requests; AWS documents the corresponding invocation permissions. [Lambda function URL access control](https://docs.aws.amazon.com/lambda/latest/dg/urls-auth.html)

The pilot uses an operator-controlled CodeBuild project and disposable jobs. Candidates can edit and execute the assessed pipeline script; the project, execution identity and account target remain operator controlled. Project concurrency is one, privileged mode is disabled and each build is limited to five minutes. External GitHub connections, personal repositories and production deployment identities are not part of this exercise. A managed lab provider remains an alternative to compare if operating cost, isolation or candidate experience proves unsuitable.

This exercise adds practical AWS, IaC, release and monitoring evidence. It does not on its own establish expertise in multi-region architecture, network design, EKS, distributed transactions or full application performance monitoring. Assess those with complementary evidence and subsequent exercise versions.

## Account, access and cost controls

The dedicated AWS lab now has organization `o-6ugwwht3qj`, lab OU `ou-xc9q-of0al9sb` and member account `689324611808`. Account identity, access and quotas were checked directly. The existing Kubernetes environment still issues no AWS administration credentials to candidates. AWS account leasing, credential boundaries, asynchronous jobs and service cleanup are separate implementations from namespace cleanup and require their own acceptance evidence.

The pilot starts with one dedicated non-production account in the sandbox OU. Additional concurrency requires additional independently leased accounts. Assessment hosting, candidate records and orchestration stay outside that child account, with no permitted candidate trust path into them. Production credentials, assessment-service credentials and customer data are excluded. Under the user's selected existing-billing-account route, the existing hosting account is also the Organizations management account; moving production into a separate member account is future governance work, not part of this rollout. AWS's reference architecture recommends separating sensitive orchestration and sandbox accounts and keeping management accounts free of operational workloads. [Sandbox deployment accounts](https://docs.aws.amazon.com/solutions/latest/innovation-sandbox-on-aws/choosing-the-deployment-account.html)

| Boundary | Initial requirement |
| --- | --- |
| Identity | Issue short-lived STS credentials only for the allocated sandbox and exercise role. Bind issuance to the active lease; stop renewal on submission/expiry. Candidate commands can inspect credentials available to their process, so assume those temporary credentials can be copied and constrain their permissions accordingly. Never expose the broker or cleanup role. |
| Services | Default-deny through the effective IAM/SCP policy set. Allow only required Lambda, S3 and CloudWatch actions, selected reads and tightly scoped application-role changes. A pipeline service is added only if selected and reviewed. Deny IAM users/access keys, Organizations administration, cross-account trust, marketplace purchases and unrelated compute/services. |
| Region | Permit one selected region after checking service availability, operating requirements and actual account quotas. Test global-service exceptions explicitly. A region condition is one part of the policy, not a complete data-location guarantee. |
| Resource scope | Restrict actions to the exercise resources, fixed names/ARNs and immutable lease ownership. Keep S3 public access blocked. Deny modification of protected logging, state, cleanup identities, boundaries and lifecycle controls. Do not rely on mutable candidate tags as the only boundary. |
| Privilege escalation | Restrict `iam:PassRole` to the application role and the intended service. Candidate-managed policies cannot exceed its protected boundary. Test resource-based policies and direct grants to role sessions as well as identity policies; boundaries alone are not a universal cap on all resource-policy grants. |
| Resource and request volume | Enforce small function concurrency, memory/runtime limits where supported, bounded artifacts/logs, pipeline job concurrency, API/job rate limits and a global cap on active leases. Protect the controls from candidate edits. IAM cannot express every desired count or size limit: inventory enforcement gaps and exclude a capability when it cannot be bounded acceptably. |
| Runtime and network | Isolate candidate execution and credentials from the app and management runtime. The current CodeBuild pilot has public outbound internet; an AWS/artifact endpoint allowlist is a future control, not a deployed claim. Bound Lambda's execution identity separately so candidate code cannot acquire management access. |
| Time and spending | Enforce the lease server-side and independently in the orchestrator; stop jobs and revoke access when it ends. Use budgets/alerts as a backstop alongside service restrictions, rate limits and measured usage. They are not a precise real-time spending cap. |

SCPs set available permission boundaries but do not grant permissions. AWS's SCP syntax includes region restrictions with exceptions for required global services. Evaluate the full effective policy set using real allowed and denied calls before opening the lab. [Organizations security guidance](https://docs.aws.amazon.com/prescriptive-guidance/latest/security-reference-architecture/organizations-security.html), [SCP syntax and region example](https://docs.aws.amazon.com/organizations/latest/userguide/orgs_manage_policies_scps_syntax.html)

AWS documents both permission-boundary evaluation exceptions and restricting role passing to an intended service. These are reasons to test privilege paths explicitly, rather than treating a single boundary policy as proof of isolation. [IAM permissions boundaries](https://docs.aws.amazon.com/IAM/latest/UserGuide/access_policies_boundaries.html), [IAM PassRole](https://docs.aws.amazon.com/IAM/latest/UserGuide/id_roles_use_passrole.html)

AWS Budgets follows delayed billing-data updates. Measure cost exposure between detection and shutdown, including resources that continue running after access is revoked. Reserved Lambda concurrency can limit concurrent executions, but does not by itself cap total requests, storage, logs or account expenditure. [AWS Budgets update frequency](https://docs.aws.amazon.com/cost-management/latest/userguide/budgets-best-practices.html), [Lambda concurrency](https://docs.aws.amazon.com/lambda/latest/dg/configuration-concurrency.html)

## Lease completion and account reuse

Use an explicit lifecycle: available → reserved → preparing → ready → frozen → cleaning → validating → available. Any ambiguous or failed cleanup moves the account to quarantine. A lease cannot be reset by the candidate to hide evidence; an authorised retake uses a new recorded lease.

On submission or expiry, stop admission of new work, disable credential renewal, cancel or fence running jobs, revoke existing role sessions, collect an independent final-state record and then clean the sandbox. Capture the last available evidence if a service is unreachable and mark missing evidence explicitly. Retain evidence under the assessment's existing access/retention rules, with secret redaction; do not retain reusable credentials.

Revoking a role's existing sessions does not prevent fresh sessions issued after the revocation cutoff. The orchestrator must also block renewal and reassignment until revocation and cleanup are verified. Use a lease-specific identity or an exclusive account role so revocation cannot interrupt another candidate. Measure permission propagation rather than promising instantaneous cutoff. [Revoking role sessions](https://docs.aws.amazon.com/IAM/latest/UserGuide/id_roles_use_revoke-sessions.html)

Do not treat `terraform destroy` as proof of a clean account. A candidate can create resources outside Terraform, change state or interrupt a job midway. Enumerate the allowed services through their own APIs, remove untracked objects and versions, inspect role policies/trust, remove application logs according to evidence retention, and compare with the protected baseline. Confirm no pending job can recreate resources. Retry a bounded number of times, then quarantine and alert the operator; never reassign an uncertain account.

AWS Innovation Sandbox offers account leasing and recycling worth evaluating as an orchestration option. Its documentation explicitly notes that Resource Explorer does not index every resource type. Select quarantine on unexpected leftovers and supplement any generic cleanup product with exercise-specific inventories. Measure cleanup coverage, cooldown, failure handling and operator effort. [Account Cleaner validation](https://docs.aws.amazon.com/solutions/latest/innovation-sandbox-on-aws/account-cleaner-component.html)

Maintain a pre-provisioned account pool rather than assuming account creation/closure is instantaneous. Review the actual organization's account, creation, closure and API quotas before setting cohort capacity. Account closure is not the per-candidate reset mechanism. [AWS Organizations quotas](https://docs.aws.amazon.com/organizations/latest/userguide/orgs_reference_limits.html)

## Phases and acceptance gates

The thresholds below are proposed acceptance targets, not observed performance or provider promises. The currently deployed account supports one exclusive synthetic lease. Agree the engineer-pilot cohort size, peak concurrency and spending envelope before broadening access beyond the named fictional checks.

Current progress: phase 1 technical acceptance is complete for the controlled two-lab Kubernetes pilot. The authorized dedicated AWS account and CodeBuild prototype are deployed, and one full synthetic AWS operator run has passed the recorded 24-check matrix. Two earlier runs exposed the documented Lambda execution-role refresh requirement and were independently cleaned up. Alpha's combined candidate/assessor path and Beta's closed-browser lab expiry/reopened-page path passed with independently verified resource absence. Administrative candidate finalization and elapsed-time reporting have the documented return-request limitation. The proposed repeated-trial and broader fault gates below have not been met by these checks. The engineer study, managed-provider cost comparison, scale/cost trials and hiring calibration remain incomplete. AWS jobs use their own asynchronous lifecycle instead of Kubernetes' 20-second command timeout.

| Phase | Deliverable | Gate before proceeding |
| --- | --- | --- |
| 1. Kubernetes operator validation | Deploy the Kubernetes runner in a dedicated test environment; exercise bootstrap, repair, command recording, expiry and cleanup. | Every seeded defect is reproducible. Candidate separation, forbidden network/RBAC operations, duplicate commands, disconnections, timeout and expiry tests pass. Every accepted command has a terminal record or explicit reconciliation failure. No unverified namespace is considered clean. |
| 2. Kubernetes engineer pilot | A small supervised cohort, including experienced and less-experienced engineers; independent marking and a short usability interview. | Report bootstrap p50/p95, command latency/error rates, final-evidence completeness, cleanup time/failure rate, operator intervention and cost. Review where candidates were blocked by the console or misunderstood the problem. Resolve score disagreements and instruction ambiguity before broader hiring use. |
| 3. AWS design and provider comparison | The owned pilot's bounded exercise, policy matrix and account/job lifecycle are implemented. A managed-provider comparison remains future work. | Validate the chosen pilot's exclusive allocation, temporary access, evidence, expiry, denied escalation and cleanup. Compare measured usage/support effort with actual provider quotes before a broader rollout decision. |
| 4. AWS internal prototype | Asynchronous jobs, exclusive account leasing, scoped credentials, versioned source storage, assessor snapshots and cleanup reconciliation are implemented. Immutable exercise binding remains pending. | Proposed prewarmed bootstrap p95 ≤120 seconds; report cold starts separately. Run at least 20 complete lifecycle trials, including injected failures. No accepted mutation lost or duplicated; no cross-candidate or assessment-service access. Failed cleanup always quarantines. Record time from lease end to effective access denial and to verified reset. |
| 5. AWS assessment pilot | Supervised candidate tasks, realistic peak-concurrency test and cost reconciliation after billing data settles. | Meet the agreed concurrency and cost envelope without borrowing production capacity. No unexplained scoring failures, evidence gaps or account reuse failures. Set final duration and pass criteria from engineer performance and blind marking. Define the operator response and candidate accommodation for infrastructure failures. |
| 6. Controlled release | Feature flag, bounded cohort size, capacity reservation, alerts, operator runbook and retake policy. | Named operational owner; tested stop switch and quarantine recovery; capacity available before candidate invitations; measured support/cost acceptable. Broaden services or add AWS Console access only through a new reviewed exercise version. |

Include these fault/abuse cases in the live validation matrix: replayed request IDs; simultaneous starts; another candidate's session/account identifiers; altered account/region targets; role chaining and PassRole escalation; attempts to remove a boundary or disable logging; public/cross-account resource grants; excessive invocation/logging/storage; background work after timeout; copied credentials after submission; edited Terraform state; deletion protection or versioned objects obstructing cleanup; orchestrator restart; partially failed apply; missing final snapshot; and resources reappearing after a nominal cleanup.

For candidate fairness, separate a platform outage from a candidate's technical mistake. Record lost lab time and the reason for an operator-authorised retake. Do not change a running assessment's timer or infer a failed competency silently. The current Kubernetes timeout policy is one specific item to evaluate before carrying it into AWS.

## Measurements that decide the provider and rollout

Keep a single pilot scorecard for both Kubernetes and AWS. Report sample size and failures alongside percentiles; a small cohort does not establish production reliability.

| Measurement | How to collect it | Decision it informs |
| --- | --- | --- |
| Bootstrap and responsiveness | Time from accepted start to a verified usable environment; cold/warm p50/p95; queued versus running job time. | Prewarming, reserved capacity and whether the assessment timer needs an explicit preparation stage. |
| Cleanup and reuse | Lease end, effective credential denial, last workload stopped, final evidence saved, inventory verification and account available timestamps; quarantine counts. | Pool size, reset policy and operational staffing. |
| Concurrency | Simulate the agreed cohort peak plus a documented burst; record AWS throttling, runner queues and rejected starts. | Safe invitation limits and admission controls. |
| Recording | Compare accepted request IDs, job logs, artifacts and independent cloud state across retries/restarts. Include truncation, redaction and missing-data counts. | Whether assessors have complete, attributable evidence. |
| Assessment quality | Completion time, infrastructure-caused lost time, ambiguous instructions, observed alternatives, blind marking agreement and candidate feedback. | Exercise scope, editor/console design, rubric, timing and pass standard. |
| Cost | Per-account/service usage, direct session cost, idle-pool allocation, orchestration/logging/storage, cleanup failures and operator time; reconcile after billing data settles. | Managed-provider versus owned-orchestration choice and permitted cohort size. |

Calculate cost per completed assessment using total attributable lab cost divided by completed sessions, and report failed/abandoned sessions separately so their cost is not hidden. Obtain current regional AWS prices or a dated provider quote when building the pilot budget. This plan assumes neither free-tier eligibility nor a fixed per-session price.

The combined synthetic browser checks are complete within the documented lifecycle limits. The next decision is whether the recorded controls, immutable artifact-binding work and administrative status/time-reporting follow-up support a separately reviewed supervised engineer study. That study must establish candidate experience, marking consistency and practical timing; further trials must establish scale, reset reliability and cost before broader release. The successful operator and browser checks are not evidence of human marking validity, production-scale reliability or a per-session cost guarantee. This document does not itself authorize an account cleanup operation.
